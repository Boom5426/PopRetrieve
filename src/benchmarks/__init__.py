"""HIR-Bench — Heterogeneous Inverse Retrieval Benchmark.

Modules:
    heterogeneous_retrieval_benchmark  — synthetic population generator
    oracle_utility                     — welfare functions + oracle decisions
    preference_conflict                — conflict scores (topk_disagreement, kendall)
    theory_boundary                    — analytical flip boundary (α* = B/(A+B))
    predictability                     — leakage-safe feature extraction + prediction
"""
